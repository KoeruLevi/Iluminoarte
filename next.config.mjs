/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: { typedRoutes: true },
  // quita: output: 'export'
  images: { /* si quieres volver a optimizar */ }
};
export default nextConfig;